version: 0.2

phases:
  install:
    runtime-versions:
      docker: 19
    commands:
      - echo Using Docker Images
  pre_build:
   commands: 
      - echo Docker Login
      - docker login --username securetest1 --password Arseetha@12
   finally:
      - echo The scan is completed 
 
