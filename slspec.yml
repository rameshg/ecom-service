version: 0.2

env:
  secrets-manager:
    Password: "Docker-Cred:PASSWORD"
    Username: "Docker-Cred:USER_NAME"
phases:
  install:
    runtime-versions:
      docker: 19
    commands:
      - cho "Quitting build installations." && exit 1
  pre_build:
    commands:
      - echo pre_build step...
     
 
