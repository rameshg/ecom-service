version: 0.2

env:
  variables:
    APP_ID: a040441301daa0781830f3f1a020a98132503cfa5a8ae1095483485eb6f89daf
phases:
  install:
    commands:
      - echo Using Powershell version 7.0.2
  pre_build:
   commands: 
      - Build with Maven
      - mvn --batch-mode --update-snapshots verify
      - echo Scan Initiated
      - docker login --username $USER_NAME --password $PASSWORD
      - docker pull securetest1/brakeman
   finally:
      - echo The scan is completed 
 
