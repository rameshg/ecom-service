version: 0.2

env:
  secrets-manager:
    Password: "Docker-Cred:PASSWORD"
    Username: "Docker-Cred:USER_NAME"
phases:
  install:
    runtime-versions:
      docker: 19
    commands:
      - echo "Quitting build installations." 
  pre_build:
    commands:
      - echo "Executing pre_build steps..." && exit 1
  build:
    commands:
      - echo "Executing build steps.."
     
 
